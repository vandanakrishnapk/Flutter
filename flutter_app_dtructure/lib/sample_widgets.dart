import 'package:flutter/material.dart';

class SampleWidgets extends StatelessWidget {
  const SampleWidgets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:SizedBox(height: 200,width: 200,
      child: Column(

      )),),
    );
  }
}
